# Security policy

Report vulnerabilities privately through GitHub security advisories. Never attach production checkpoints, prompts, tokens, or personal data.

The header identity adapter is for local demonstration. Replace it with verified OIDC/JWT claims before deployment. For multi-instance production deployments, replace SQLite with the official PostgreSQL checkpointer and a transactional outbox-backed tool executor.

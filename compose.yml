services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - agent-data:/data
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
volumes:
  agent-data:
